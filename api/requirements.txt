# Runtime dependencies — EXACT pins (==) so dev and prod resolve to IDENTICAL versions on
# every machine and every deploy (ADR-048).
#
# Why exact: a loose `pandas>=2.2,<3` let the dev laptop resolve to pandas 3.0 while Railway
# resolved to 2.x — BOTH "valid" — and that silent gap changed ALGORITHMIC COMPLEXITY (an
# index lookup that is O(1) on pandas 3.0 is O(n) on 2.x), hiding an 80x O(n^2) regression for
# weeks (ADR-047). Never widen a pin without re-testing under the deployed Python (3.12).
# To bump: change here deliberately, reinstall dev from this file, run the suite, and confirm
# results are byte-identical. Standardised on PROD's versions (move dev to match prod, not the
# reverse — production is the environment that must not break; verified result-identical).
#
# Python is pinned to 3.12 via api/.python-version (Nixpacks pins the minor; the patch floats —
# /env reports the live patch). The versions below are what Python 3.12 resolves today.

fastapi==0.139.0
uvicorn[standard]==0.51.0
starlette==1.3.1          # fastapi's ASGI core — pinned so the web stack can't drift
pydantic==2.13.4          # fastapi's model layer — ""
anyio==4.14.1             # starlette/async core — ""
pandas==2.3.3             # <-- the pin that ends the O(n^2)-hiding drift (was pandas>=2.2,<3)
numpy==2.5.1
requests==2.34.2
pyarrow==24.0.0           # read the full-history Parquet dataset (ADR-035)

# Edge-validation library (Monte Carlo + temporal-stability + verdict). Installed from git at
# deploy; Railway must reach GitHub during pip install. Pinned to an exact commit for
# reproducible deploys (back-tester main HEAD = v0.2.0 repackage). Bump this sha deliberately.
backtester @ git+https://github.com/jimmuell/back-tester.git@ef6fb684d482832f02a7f906cf01acc8e853c207
