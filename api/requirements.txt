fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
# Edge-validation library (Monte Carlo + temporal-stability + verdict). Installed
# from git at deploy; Railway must be able to reach GitHub during pip install.
# For reproducibility this may later be pinned to @<commit-sha> instead of @main.
backtester @ git+https://github.com/jimmuell/back-tester.git@main
