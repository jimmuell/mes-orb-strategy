# Dev/test-only dependencies (NOT installed on Railway deploy — see requirements.txt).
# Run the engine test suite with:  cd api && python -m pytest -v
-r requirements.txt
pytest==9.0.3    # bumped from 8.4.2 for PYSEC-2026-1845 (ADR-049); dev/CI-only, not shipped
httpx==0.28.1    # required by fastapi.testclient.TestClient (ADR-039 import/health smoke test)
anthropic==0.120.0  # dev/CI-only; the LIVE extraction call runs in Supabase, not on the Railway
                    # runtime (see requirements.txt header). Used by the gated extraction harness.
